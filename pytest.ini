[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: đánh thật ra mạng nhưng MIỄN PHÍ (gTTS, edge-tts). Chạy trước mỗi lần deploy.
    paid: đánh thật ra mạng và TỐN TIỀN (Gemini). Chỉ chạy khi cần kiểm chứng nhà cung cấp.
addopts = -m "not integration and not paid"
